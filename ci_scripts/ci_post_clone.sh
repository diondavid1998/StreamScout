#!/bin/sh
# Xcode Cloud — runs once after the repo is cloned, before the build.
# Add any pre-build setup here (e.g. installing tools, resolving deps).
set -e

echo "▶ ci_post_clone: nothing extra needed for this SwiftUI app"
